[workspace]
name = "testing_ems"
version = "0.1.0"
description = "EMS superquadric fitting bridge for shape completion experiments"
channels = ["conda-forge"]
platforms = ["osx-arm64", "osx-64", "linux-64"]

[dependencies]
python = "3.11.*"
numpy = ">=1.24"
scipy = ">=1.10"
numba = ">=0.57"
scikit-learn = ">=1.3"
plyfile = ">=0.9"
pyyaml = ">=6.0"
matplotlib = ">=3.7"
pip = "*"

[tasks]
install-ems = "pip install -e vendor/EMS-superquadric_fitting/Python --no-deps"
reproduce = { cmd = "python scripts/reproduce_paper_demo.py", depends-on = ["install-ems"] }
visualize = "python scripts/visualize_fit.py"
run-single = { cmd = "python run.py --partial data/vendor/single/partial_pointCloud_example_1.ply --completed data/vendor/single/partial_pointCloud_example_1.ply --mode single", depends-on = ["install-ems"] }
run-batch = { cmd = "python run.py --batch data", depends-on = ["install-ems"] }

[pypi-dependencies]
ems-bridge = { path = ".", editable = true }
